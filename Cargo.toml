[package]
name = "data-raw"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.149" }
rootcause = "0.12.1"
mlua = { version = "0.11.6", features = ["lua52", "module"] }